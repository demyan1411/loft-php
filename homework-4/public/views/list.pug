extends layouts/layout

block content
  nav.navbar.navbar-inverse.navbar-fixed-top
    .container
      .navbar-header
        button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#navbar', aria-expanded='false', aria-controls='navbar')
          span.sr-only Toggle navigation
          span.icon-bar
          span.icon-bar
          span.icon-bar
        a.navbar-brand(href='#') Project name
      #navbar.collapse.navbar-collapse

        include blocks/menu


      // /.nav-collapse
  .container
    h1 Запретная зона, доступ только авторизированному пользователю
    h2 Информация выводится из базы данных
    table#users.table.table-bordered
      tr
        th Пользователь(логин)
        th Имя
        th возраст
        th описание
        th Фотография
        th Действия

      each user in users
        tr
          each val, key in user
            td
              if key == 'photo' && !empty(val)
                img(src='/photos/#{val}', alt='')

              if key != 'photo'
                div #{val}

          td
            a.delete-user(href='/delete-user' data-login='#{user["login"]}') Удалить пользователя